# weaving webs of wonder


## Adding a new page

1. Make a folder under `webpages-workshop/` containing an `index.html`.
2. Add an entry to `hotspots.json` keyed by the folder name.
3. Run `node build.js` and commit `pages.json`.
